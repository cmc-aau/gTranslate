.. code-block:: bash

    # NOTE: replace 0.0.2 with the version you wish to install

    # using conda
    conda create -n gtdbtk-0.0.2 -c conda-forge -c bioconda gtdbtk=0.0.2

    # using mamba (alternative)
    mamba create -n gtdbtk-0.0.2 -c conda-forge -c bioconda gtdbtk=0.0.2
