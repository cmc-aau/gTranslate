.. code-block:: bash

    # NOTE: replace 0.0.2 with the version you wish to install

    # using conda
    conda create -n gtranslate-0.0.2 -c conda-forge -c bioconda gtdb-gtranslate=0.0.2

    # using mamba (alternative)
    mamba create -n gtranslate-0.0.2 -c conda-forge -c bioconda gtdb-gtranslate=0.0.2
